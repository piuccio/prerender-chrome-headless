{
  "name": "prerender-chrome-headless",
  "version": "1.0.0",
  "description": "Pre-render single page applications to static HTML file",
  "keywords": [
    "pre-render",
    "static",
    "headless",
    "react"
  ],
  "homepage": "https://github.com/en-japan-air/prerender-chrome-headless",
  "license": "MIT",
  "main": "index.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/en-japan-air/prerender-chrome-headless"
  },
  "scripts": {
    "lint": "eslint index.js",
    "test": "DEBUG=prerender jest",
    "test:watch": "DEBUG=prerender jest --watchAll",
    "posttest": "npm run lint"
  },
  "dependencies": {
    "chrome-launcher": "^0.3.1",
    "chrome-remote-interface": "^0.24.3",
    "debug": "^2.6.8"
  },
  "engines": {
    "node": ">=6.0"
  },
  "devDependencies": {
    "eslint": "^4.3.0",
    "jest": "^20.0.4"
  }
}
